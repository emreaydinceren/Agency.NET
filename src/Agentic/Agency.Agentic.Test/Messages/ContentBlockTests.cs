namespace Agency.Agentic.Test.Messages;

// ContentBlock, TextBlock, ToolUseBlock, AgentMessage and related types were removed when the
// project migrated to Microsoft.Extensions.AI.IChatClient. The equivalent M.E.AI types
// (TextContent, FunctionCallContent, ChatMessage) are part of the SDK and do not require
// separate unit tests here.
