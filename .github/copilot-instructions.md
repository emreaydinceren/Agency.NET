# Copilot Instructions

## Project Guidelines
- Prefer environment-specific configuration over an explicit IsCI flag in test setup.
- In Agency.Harness.Console output APIs, prefer named Spectre color strings (CSS-like color names) instead of ConsoleColor enum values.